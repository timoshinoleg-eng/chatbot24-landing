# ChatBot24

Современный блог на Next.js 14 с автоматизацией контента из Telegram. Платформа для публикации статей о чат-ботах, ИИ и автоматизации бизнеса с автоматическим рерайтингом через OpenRouter AI.

## 📋 Описание проекта

ChatBot24 — это полнофункциональная платформа для ведения блога с автоматической интеграцией Telegram-канала. Система автоматически:
- Получает сообщения из Telegram-канала
- Переписывает контент с помощью AI (OpenRouter)
- Подбирает изображения через Unsplash API
- Сохраняет в базу данных для модерации
- Публикует на сайте после одобрения администратором

## ✨ Функциональные возможности

### Для посетителей
- 📰 Просмотр опубликованных статей блога
- 🔍 Поиск по статьям и фильтрация по тегам
- 📄 SEO-оптимизированные страницы статей
- 📱 Адаптивный дизайн для всех устройств
- 📝 Форма обратной связи с уведомлениями в Telegram

### Для администраторов
- 🔐 Аутентификация через GitHub OAuth
- 📊 Панель управления контентом
- ✅ Модерация статей (одобрение/отклонение)
- 🗑️ Удаление неактуальных публикаций
- 📈 Статистика просмотров статей

### Автоматизация
- 🤖 Автоматический импорт из Telegram-канала
- ✍️ AI-рерайтинг контента с SEO-оптимизацией
- 🖼️ Автоподбор изображений по тегам
- ⏰ Cron-задачи для регулярной синхронизации

## 🛠 Технологический стек

| Категория | Технологии |
|-----------|------------|
| **Фреймворк** | Next.js 14 (App Router) |
| **Язык** | TypeScript 5 |
| **Стилизация** | Tailwind CSS 3.4 |
| **База данных** | PostgreSQL (Vercel Postgres) |
| **ORM** | Prisma 5 |
| **Аутентификация** | NextAuth.js 5 (Auth.js) |
| **Валидация** | Zod |
| **Хостинг** | Vercel |

### Внешние API
- **Telegram Bot API** — получение сообщений из канала
- **OpenRouter AI** — рерайтинг контента (Mistral 7B)
- **Unsplash API** — поиск изображений
- **GitHub OAuth** — аутентификация администраторов

## 📦 Установка и запуск

### Предварительные требования
- Node.js 18+ 
- npm или yarn
- PostgreSQL база данных (локальная или Vercel Postgres)

### 1. Клонирование репозитория

```bash
git clone https://github.com/your-username/chatbot24-nextjs.git
cd chatbot24-nextjs
```

### 2. Установка зависимостей

```bash
npm install
# или
yarn install
```

### 3. Настройка переменных окружения

Скопируйте файл `.env.example` в `.env.local`:

```bash
copy .env.example .env.local
```

Заполните необходимые переменные:

```env
# База данных
DATABASE_URL="postgresql://user:password@localhost:5432/chatbot24?schema=public"

# NextAuth.js
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-super-secret-key-min-32-characters"

# GitHub OAuth
GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"
ADMIN_GITHUB_USERNAME="your-github-username"

# Telegram
TELEGRAM_BOT_TOKEN="your-telegram-bot-token"
TELEGRAM_NOTIFICATION_CHAT_ID="your-chat-id-for-notifications"

# OpenRouter AI
OPENROUTER_API_KEY="your-openrouter-api-key"

# Unsplash
UNSPLASH_ACCESS_KEY="your-unsplash-access-key"

# Cron защита
CRON_SECRET="your-cron-secret-key"

# Приложение
NEXT_PUBLIC_APP_URL="http://localhost:3000"
```

### 4. Настройка базы данных

#### Генерация Prisma клиента

```bash
npm run db:generate
```

#### Применение миграций

```bash
npm run db:migrate
```

Или для продакшена:

```bash
npx prisma migrate deploy
```

#### Просмотр базы данных (опционально)

```bash
npm run db:studio
```

### 5. Запуск сервера разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

### 6. Сборка для продакшена

```bash
npm run build
npm start
```

## 🔐 Настройка Telegram бота

### Создание бота

1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный токен в `TELEGRAM_BOT_TOKEN`

### Добавление бота в канал

1. Добавьте бота в ваш Telegram-канал как администратора
2. Убедитесь, что у бота есть права на чтение сообщений
3. Укажите ID канала в файле `app/api/cron/sync-telegram/route.ts`:

```typescript
const TELEGRAM_CHANNEL_ID = '@your_channel_name';
```

### Настройка уведомлений

1. Напишите [@userinfobot](https://t.me/userinfobot), чтобы узнать свой ID
2. Добавьте ID в переменную `TELEGRAM_NOTIFICATION_CHAT_ID`
3. Или используйте ID группы/канала для уведомлений

## ⏰ Настройка Cron-задач

### Локальная разработка

Для тестирования cron-задач используйте curl:

```bash
curl -X GET "http://localhost:3000/api/cron/sync-telegram" \
  -H "Authorization: Bearer your-cron-secret"
```

### Vercel Cron Jobs

Добавьте в `vercel.json`:

```json
{
  "crons": [
    {
      "path": "/api/cron/sync-telegram",
      "schedule": "0 */6 * * *"
    }
  ]
}
```

Это запустит синхронизацию каждые 6 часов.

## 🚀 Деплой на Vercel

### Быстрый деплой

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new)

### Ручной деплой

1. Зарегистрируйтесь на [Vercel](https://vercel.com)
2. Импортируйте репозиторий GitHub
3. Добавьте переменные окружения в настройках проекта
4. Подключите Vercel Postgres в разделе Storage
5. Запустите деплой

Подробная инструкция в файле [DEPLOYMENT.md](./DEPLOYMENT.md)

## 📁 Структура проекта

```
chatbot24-nextjs/
├── app/                      # Next.js App Router
│   ├── api/                  # API Routes
│   │   ├── admin/posts/      # Управление постами (admin)
│   │   ├── auth/[...nextauth]/  # Аутентификация
│   │   ├── blog/posts/       # Публичные посты
│   │   ├── cron/sync-telegram/ # Cron задача
│   │   └── submit/           # Форма обратной связи
│   ├── globals.css           # Глобальные стили
│   ├── layout.tsx            # Корневой layout
│   └── providers.tsx         # React провайдеры
├── components/               # React компоненты
│   ├── blog/                 # Компоненты блога
│   ├── home/                 # Компоненты главной
│   ├── layout/               # Layout компоненты
│   ├── seo/                  # SEO компоненты
│   └── ui/                   # UI компоненты
├── lib/                      # Утилиты и клиенты
│   ├── openrouter.ts         # OpenRouter клиент
│   ├── prisma.ts             # Prisma клиент
│   ├── telegram-parser.ts    # Telegram парсер
│   ├── unsplash.ts           # Unsplash клиент
│   └── utils.ts              # Вспомогательные функции
├── prisma/
│   └── schema.prisma         # Схема базы данных
├── .env.example              # Пример переменных окружения
├── next.config.js            # Конфигурация Next.js
├── tailwind.config.ts        # Конфигурация Tailwind
└── package.json              # Зависимости
```

## 🔧 API Endpoints

| Endpoint | Метод | Описание | Авторизация |
|----------|-------|----------|-------------|
| `/api/blog/posts` | GET | Получить публикации | Не требуется |
| `/api/blog/posts?slug=xxx` | GET | Получить статью по slug | Не требуется |
| `/api/admin/posts` | GET | Список всех постов | Admin |
| `/api/admin/posts` | PATCH | Обновить статус поста | Admin |
| `/api/admin/posts?id=xxx` | DELETE | Удалить пост | Admin |
| `/api/cron/sync-telegram` | GET/POST | Синхронизация с Telegram | Cron Secret |
| `/api/submit` | POST | Отправка формы | Не требуется |
| `/api/auth/[...nextauth]` | ALL | Аутентификация | NextAuth |

## 📝 Лицензия

MIT License — см. файл [LICENSE](./LICENSE)

## 👨‍💻 Автор

ChatBot24 Team

## 📞 Поддержка

При возникновении проблем создайте Issue в репозитории или свяжитесь через форму на сайте.

---

**Примечание**: Для работы AI-рерайтинга требуется API ключ OpenRouter. Бесплатные модели доступны с ограничениями по количеству запросов.
