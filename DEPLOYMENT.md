# Руководство по деплою ChatBot24

Подробная инструкция по развертыванию проекта на Vercel с настройкой всех необходимых сервисов.

## 📋 Содержание

1. [Подготовка](#подготовка)
2. [Настройка базы данных](#настройка-базы-данных)
3. [Настройка GitHub OAuth](#настройка-github-oauth)
4. [Настройка Telegram бота](#настройка-telegram-бота)
5. [Деплой на Vercel](#деплой-на-vercel)
6. [Настройка переменных окружения](#настройка-переменных-окружения)
7. [Настройка Cron-задач](#настройка-cron-задач)
8. [Верификация деплоя](#верификация-деплоя)

---

## Подготовка

### Требования

- Аккаунт на [Vercel](https://vercel.com)
- Аккаунт на [GitHub](https://github.com)
- Аккаунт на [Telegram](https://telegram.org)
- Аккаунт на [OpenRouter](https://openrouter.ai) (для AI-рерайтинга)
- Аккаунт на [Unsplash](https://unsplash.com/developers) (для изображений)

### Репозиторий

Убедитесь, что код проекта загружен в публичный или приватный репозиторий GitHub.

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/username/chatbot24-nextjs.git
git push -u origin main
```

---

## Настройка базы данных

### Vercel Postgres (рекомендуется)

1. Зайдите в [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите или создайте проект
3. Перейдите во вкладку **Storage**
4. Нажмите **Create Database** → **Postgres**
5. Выберите регион (рекомендуется ближайший к вашей аудитории)
6. Нажмите **Create**
7. После создания перейдите в **.env.local**
8. Скопируйте значение `POSTGRES_URL` — это будет ваш `DATABASE_URL`

### Внешняя база данных

Если используете внешний PostgreSQL:

```env
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"
```

---

## Настройка GitHub OAuth

### Создание OAuth App

1. Перейдите в **Settings** вашего GitHub-аккаунта
2. Внизу слева выберите **Developer settings**
3. Нажмите **OAuth Apps** → **New OAuth App**
4. Заполните форму:
   - **Application name**: ChatBot24
   - **Homepage URL**: `https://your-domain.vercel.app`
   - **Application description**: Авторизация для админ-панели
   - **Authorization callback URL**: `https://your-domain.vercel.app/api/auth/callback/github`

5. Нажмите **Register application**
6. Скопируйте **Client ID**
7. Нажмите **Generate a new client secret** и скопируйте секрет

### Права доступа

В разделе **Permissions** убедитесь, что выбраны:
- `read:user` — чтение информации о пользователе
- `user:email` — доступ к email

---

## Настройка Telegram бота

### Создание бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Укажите название бота (например, "ChatBot24 Helper")
4. Укажите username бота (например, `chatbot24_helper_bot`) — должен заканчиваться на `_bot`
5. Сохраните полученный **токен**

### Добавление бота в канал

1. Откройте ваш Telegram-канал
2. Добавьте бота администратором:
   - Управление каналом → Администраторы → Добавить администратора
   - Найдите вашего бота по username
   - Права: **Только просмотр сообщений** (минимально необходимые)

### Получение ID канала

1. Отправьте любое сообщение в канал
2. Откройте в браузере:
   ```
   https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
   ```
3. Найдите `chat.id` в ответе — это ID канала (может быть отрицательным числом или `@channelname`)

### Настройка уведомлений

1. Напишите [@userinfobot](https://t.me/userinfobot), чтобы узнать свой ID
2. Или добавьте бота в группу и используйте ID группы
3. Укажите этот ID в `TELEGRAM_NOTIFICATION_CHAT_ID`

---

## Деплой на Vercel

### Способ 1: Через Dashboard

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите **Add New Project**
3. Импортируйте репозиторий с GitHub
4. Настройте:
   - **Framework Preset**: Next.js
   - **Root Directory**: `./` (или подпапка, если monorepo)
5. Нажмите **Deploy**

### Способ 2: Через CLI

```bash
# Установка Vercel CLI
npm i -g vercel

# Логин
vercel login

# Деплой
vercel --prod
```

### Способ 3: Git интеграция

1. Подключите репозиторий GitHub к Vercel
2. Каждый push в `main` будет автоматически деплоиться
3. Pull Request получают Preview deployments

---

## Настройка переменных окружения

В **Project Settings** → **Environment Variables** добавьте:

### Обязательные переменные

| Переменная | Значение | Описание |
|------------|----------|----------|
| `DATABASE_URL` | `postgres://...` | URL базы данных |
| `NEXTAUTH_URL` | `https://your-domain.vercel.app` | URL приложения |
| `NEXTAUTH_SECRET` | `random-string-32-chars` | Секрет для JWT (генератор ниже) |
| `GITHUB_CLIENT_ID` | `Iv1.xxx...` | GitHub OAuth Client ID |
| `GITHUB_CLIENT_SECRET` | `xxx...` | GitHub OAuth Secret |
| `ADMIN_GITHUB_USERNAME` | `your-username` | GitHub username администратора |

### Переменные для Telegram

| Переменная | Значение | Описание |
|------------|----------|----------|
| `TELEGRAM_BOT_TOKEN` | `123456:ABC...` | Токен от @BotFather |
| `TELEGRAM_NOTIFICATION_CHAT_ID` | `123456789` | ID для уведомлений |

### Переменные для AI и изображений

| Переменная | Значение | Описание |
|------------|----------|----------|
| `OPENROUTER_API_KEY` | `sk-or-v1-...` | API ключ OpenRouter |
| `UNSPLASH_ACCESS_KEY` | `xxx...` | Access Key Unsplash |

### Защита Cron

| Переменная | Значение | Описание |
|------------|----------|----------|
| `CRON_SECRET` | `random-secret` | Секрет для авторизации cron |

### Генерация секретов

Для `NEXTAUTH_SECRET` и `CRON_SECRET`:

```bash
# Linux/Mac
openssl rand -base64 32

# Windows PowerShell
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 } | ForEach-Object { [byte]$_ }))

# Или онлайн: https://generate-secret.vercel.app/32
```

---

## Настройка Cron-задач

### Создание `vercel.json`

В корне проекта создайте файл `vercel.json`:

```json
{
  "crons": [
    {
      "path": "/api/cron/sync-telegram",
      "schedule": "0 */6 * * *"
    }
  ]
}
```

### Расписания

| Выражение | Описание |
|-----------|----------|
| `0 */6 * * *` | Каждые 6 часов |
| `0 */12 * * *` | Каждые 12 часов |
| `0 0 * * *` | Каждый день в полночь |
| `0 0 * * 1` | Каждый понедельник |

### Ручной запуск

```bash
# Запуск синхронизации вручную
curl -X GET "https://your-domain.vercel.app/api/cron/sync-telegram" \
  -H "Authorization: Bearer your-cron-secret"
```

---

## Верификация деплоя

### Проверка работоспособности

1. **Главная страница**
   - Откройте `https://your-domain.vercel.app`
   - Убедитесь, что страница загружается без ошибок

2. **Аутентификация**
   - Перейдите на `https://your-domain.vercel.app/auth/signin`
   - Попробуйте войти через GitHub
   - Должен работать только указанный `ADMIN_GITHUB_USERNAME`

3. **API endpoints**
   ```bash
   # Проверка публичных постов
   curl "https://your-domain.vercel.app/api/blog/posts"
   
   # Проверка cron (должен вернуть 401 без авторизации)
   curl "https://your-domain.vercel.app/api/cron/sync-telegram"
   ```

4. **База данных**
   - Подключитесь к базе через Prisma Studio:
   ```bash
   npx prisma studio
   ```
   - Убедитесь, что таблицы созданы

### Проверка Cron-задач

В Vercel Dashboard:
1. Перейдите в **Deployments**
2. Выберите последний деплой
3. Откройте **Cron Jobs**
4. Убедитесь, что задача отображается

### Мониторинг

1. **Logs**: Vercel Dashboard → Deployments → [Deployment] → Functions
2. **Analytics**: Вкладка **Analytics** для просмотра посещаемости
3. **Web Vitals**: Метрики производительности

---

## Устранение неполадок

### Ошибка "Database connection failed"

- Проверьте `DATABASE_URL` в переменных окружения
- Убедитесь, что IP Vercel разрешён в настройках базы
- Для Vercel Postgres проверьте интеграцию в разделе Storage

### Ошибка "Authentication failed"

- Проверьте `NEXTAUTH_SECRET` (минимум 32 символа)
- Убедитесь, что `NEXTAUTH_URL` соответствует домену
- Проверьте GitHub OAuth Callback URL

### Ошибка "Unauthorized" при входе

- Убедитесь, что `ADMIN_GITHUB_USERNAME` указан правильно (без @)
- Проверьте, что пользователь имеет доступ к GitHub OAuth App

### Cron-задача не работает

- Проверьте `CRON_SECRET` в переменных окружения
- Убедитесь, что `vercel.json` корректен
- Проверьте логи в Vercel Dashboard

### Telegram бот не получает сообщения

- Убедитесь, что бот добавлен в канал как администратор
- Проверьте правильность `TELEGRAM_BOT_TOKEN`
- Убедитесь, что в канале есть новые сообщения

---

## Обновление деплоя

### Автоматический деплой

При push в `main` ветку:

```bash
git add .
git commit -m "Update: новые функции"
git push origin main
```

Vercel автоматически пересоберёт проект.

### Ручной деплой

```bash
vercel --prod
```

### Миграция базы данных после обновления

```bash
# Локально
npx prisma migrate deploy

# Или через Vercel CLI
vercel env pull .env.production.local
npx prisma migrate deploy
```

---

## Полезные ссылки

- [Next.js на Vercel](https://nextjs.org/docs/deployment)
- [Vercel Postgres](https://vercel.com/docs/storage/vercel-postgres)
- [Vercel Cron Jobs](https://vercel.com/docs/cron-jobs)
- [NextAuth.js документация](https://next-auth.js.org/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
