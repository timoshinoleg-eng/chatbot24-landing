# 📚 Инструкция по работе с блогом ChatBot24

## Структура базы данных

Таблица `Post` содержит следующие поля:

| Поле | Описание | Обязательное |
|------|----------|--------------|
| `id` | Уникальный ID (CUID) | ✅ |
| `telegramMessageId` | ID сообщения из Telegram | ✅ |
| `originalChannel` | Канал-источник | ✅ |
| `originalText` | Оригинальный текст | ✅ |
| `rewrittenTitle` | Заголовок статьи | ✅ |
| `rewrittenContent` | HTML-контент статьи | ✅ |
| `summary` | Краткое описание | ✅ |
| `imageUrl` | URL картинки | ❌ |
| `imageSource` | Источник (UNSPLASH/AI_GENERATED) | ✅ |
| `tags` | Массив тегов | ✅ |
| `slug` | URL-идентификатор | ✅ |
| `status` | Статус (PENDING/PUBLISHED/REJECTED) | ✅ |
| `views` | Количество просмотров | ✅ |
| `metaTitle` | SEO-заголовок | ❌ |
| `metaDescription` | SEO-описание | ❌ |
| `publishedAt` | Дата публикации | ❌ |

---

## 📝 Способы добавления статей

### Способ 1: Через Telegram-канал (автоматический)

1. Настройте переменные окружения:
   ```
   TELEGRAM_CHANNEL_ID=@your_channel
   TELEGRAM_BOT_TOKEN=your_bot_token
   OPENROUTER_API_KEY=your_key
   UNSPLASH_ACCESS_KEY=your_key
   ```

2. Публикуйте посты в Telegram-канале

3. Бот автоматически:
   - Парсит сообщения
   - Переписывает контент через AI
   - Подбирает картинки
   - Сохраняет в базу со статусом `PENDING`

4. В админ-панели `/admin/dashboard` одобрите пост

### Способ 2: Через SQL (ручной)

```sql
INSERT INTO "Post" (
  "id",
  "telegramMessageId",
  "originalChannel",
  "originalText",
  "rewrittenTitle",
  "rewrittenContent",
  "summary",
  "imageUrl",
  "imageSource",
  "tags",
  "slug",
  "status",
  "views",
  "metaTitle",
  "metaDescription",
  "publishedAt"
) VALUES (
  gen_random_uuid(),           -- или 'custom-id'
  999999999,                   -- уникальный ID
  'manual',                    -- источник
  'Оригинальный текст',        -- для истории
  'Заголовок статьи',          -- H1
  '<p>HTML контент</p>',       -- HTML
  'Краткое описание',          -- для карточки
  'https://images.unsplash.com/...',  -- картинка
  'UNSPLASH',                  -- или 'AI_GENERATED'
  ARRAY['тег1', 'тег2'],       -- теги
  'url-identificator',         -- slug
  'PUBLISHED',                 -- статус
  0,
  'SEO заголовок',
  'SEO описание',
  NOW()
);
```

### Способ 3: Через Prisma Studio

```bash
npx prisma studio
```

Откроется веб-интерфейс для редактирования базы данных.

---

## 🎨 Форматирование контента

### Поддерживаемые HTML-теги:

```html
<h2>Заголовок раздела</h2>
<h3>Подзаголовок</h3>

<p>Обычный параграф текста.</p>

<ul>
  <li>Маркированный список</li>
  <li>Второй пункт</li>
</ul>

<ol>
  <li>Нумерованный список</li>
  <li>Второй пункт</li>
</ol>

<strong>Жирный текст</strong>
<em>Курсив</em>

<a href="https://...">Ссылка</a>

<blockquote>
  Цитата или важная мысль
</blockquote>
```

### Картинки:

Картинка задаётся через поле `imageUrl`. Рекомендуемые размеры:
- **Обложка**: 1200×630px (для соцсетей)
- **Формат**: JPG или WebP
- **Источники**: Unsplash, собственные

---

## 🔍 SEO-оптимизация

### Поля для SEO:

1. **metaTitle** — заголовок для поисковиков (50-60 символов)
2. **metaDescription** — описание (150-160 символов)
3. **slug** — ЧПУ URL (латиница, дефисы)
4. **tags** — теги для категоризации

### Пример хорошего slug:
```
✅ chatboty-dlya-magazina
❌ chatboty_dlya_magazina
❌ статья-1
```

---

## 📊 Аналитика

Просмотры считаются автоматически при открытии статьи.

Для просмотра статистики:
```sql
SELECT 
  "rewrittenTitle",
  "views",
  "publishedAt"
FROM "Post"
WHERE "status" = 'PUBLISHED'
ORDER BY "views" DESC;
```

---

## 🚀 Публикация через cron

Для автоматической публикации настройте cron в Vercel:

```json
{
  "crons": [
    {
      "path": "/api/cron/sync-telegram",
      "schedule": "0 */6 * * *"
    }
  ]
}
```

Или запустите вручную:
```bash
curl https://your-site.com/api/cron/sync-telegram?secret=CRON_SECRET
```

---

## 🐛 Отладка

Если статьи не отображаются:

1. Проверьте API:
   ```bash
   curl https://chatbot24.su/api/blog/posts
   ```

2. Проверьте статус в базе:
   ```sql
   SELECT "slug", "status", "publishedAt" 
   FROM "Post";
   ```

3. Проверьте логи Vercel (Dashboard → Logs)

---

## 💡 Лучшие практики

1. **Заголовок** — конкретный, с цифрами или выгодой
2. **Вступление** — первые 2-3 предложения цепляют
3. **Структура** — используйте подзаголовки H2/H3
4. **Списки** — для перечислений (легко сканировать)
5. **Картинки** — каждые 300-400 слов
6. **CTA** — призыв к действию в конце
7. **Теги** — 3-5 релевантных тегов

---

## 📁 Файлы для работы

- `app/api/blog/posts/route.ts` — API для получения статей
- `app/blog/page.tsx` — страница списка статей
- `app/blog/[slug]/page.tsx` — страница статьи
- `prisma/schema.prisma` — структура базы данных
- `seed-test-post.sql` — пример тестовой статьи